# Local development mock data — enables sample dispatches and bypasses the real backend.
# To disable, set VITE_USE_MOCK=false or remove this file.
# See src/api/mockData.ts for the sample data (delete that file when switching to real data).
VITE_USE_MOCK=true
